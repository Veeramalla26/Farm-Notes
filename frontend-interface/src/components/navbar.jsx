import React from "react";

const Navbar = () => (
  <nav>
    <button>Add Farm Item</button>
    <div>
      <a href="/profile">Profile</a>
      <a href="/security">Security</a>
      <a href="/logout">Logout</a>
    </div>
  </nav>
);

export default Navbar;
